{
  "name": "pancake-lottery-scheduler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "execute:start:testnet": "npx hardhat run --network testnet scripts/start-lottery.ts",
    "execute:start:mainnet": "npx hardhat run --network mainnet scripts/start-lottery.ts",
    "execute:close-draw:testnet": "npx hardhat run --network testnet scripts/close-draw-lottery.ts",
    "execute:close-draw:mainnet": "npx hardhat run --network mainnet scripts/close-draw-lottery.ts",
    "format:check": "prettier --check '*/**/*.{js,ts}'",
    "format:write": "prettier --write '*/**/*.{js,ts}'",
    "lint": "eslint '*/**/*.{js,ts}'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn format:check",
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  },
  "dependencies": {
    "winston": "^3.3.3",
    "winston-daily-rotate-file": "^4.5.5"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.1.4",
    "@commitlint/config-conventional": "^12.1.4",
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@types/node": "^15.12.1",
    "@typescript-eslint/eslint-plugin": "^4.26.0",
    "@typescript-eslint/parser": "^4.26.0",
    "eslint": "^7.28.0",
    "eslint-config-prettier": "^8.3.0",
    "ethers": "^5.3.0",
    "hardhat": "^2.3.0",
    "husky": "^6.0.0",
    "prettier": "^2.3.1",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.2"
  }
}
